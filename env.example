# Groq API key — free tier en console.groq.com
GROQ_API_KEY=

# Optional — GitHub personal access token
# Without this, the GitHub API is unauthenticated (60 req/h limit, sufficient for demo)
# Get one at: https://github.com/settings/tokens (no scopes needed for public repos)
GITHUB_TOKEN=
